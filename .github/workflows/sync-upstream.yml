name: Sync upstream

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-upstream
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Sync upstream
        run: scripts/sync-upstream.sh

      - name: Sync vendor rules
        run: scripts/sync-vendor-rules.sh

      - name: Commit and push changes
        run: |
          if [[ -z "$(git status --porcelain)" ]]; then
            echo "No changes to commit."
            exit 0
          fi

          git add -A
          if [[ -n "${SYNCED_UPSTREAM_COMMIT:-}" ]]; then
            git commit -m "sync: upstream ${SYNCED_UPSTREAM_COMMIT}"
          else
            git commit -m "sync: vendor rules"
          fi
          git push origin HEAD:master
